{
  "name": "pp-kompendium",
  "version": "1.1.0",
  "private": true,
  "description": "Et offline P&P-kompendium med figurer og sammenkædede symbolske formler.",
  "scripts": {
    "build": "node tools/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check dist/catalog.js && node --check dist/engine.js && node --check dist/geometry.js && node --check dist/solid-preview.js && node --check dist/app.js && node tools/check.cjs"
  },
  "engines": { "node": ">=18" }
}
